import { AppShell } from "@/components/layout/app-shell";
import { BulkProgress } from "@/components/property/bulk-progress";

export default function BulkProgressPage() {
  return (
    <AppShell>
      <BulkProgress />
    </AppShell>
  );
}
